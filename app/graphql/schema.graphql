type Mutation {
  # An example field added by the generator
  testField: String!
}

type Query {
  # An example field added by the generator
  testfield: String!

  # Find a post by ID
  user(id: ID!): User
}

type User {
  email: String!
  id: ID!
  name: String!
}